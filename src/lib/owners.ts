export interface OwnerProfileRecord {
  id: string;
  createdAt?: string;
  name: string;
  city?: string;
  district?: string;
  avatarUrl?: string;
  heroPhotoUrl?: string;
  bio?: string;
  petCount: number;
}
